# Declarative package list. Install with: ./install.sh --brew  (or: brew bundle)
# macOS-focused (casks are mac-only). https://github.com/Homebrew/homebrew-bundle

# --- core CLI ---
brew "git"
brew "the_silver_searcher"  # ag: FZF_DEFAULT_COMMAND + vim/ack config
brew "ack"
brew "fzf"
brew "ripgrep"
brew "wget"
brew "jq"

# --- editors ---
brew "neovim"
brew "tree-sitter-cli"

# --- language runtimes / version managers (Amber dev-setup standard) ---
brew "rbenv"         # Ruby (rbenv-managed, currently 3.2.x)
brew "ruby-build"
brew "nvm"           # Node
brew "go"

# --- GUI apps ---
cask "vimr"
cask "neovide-app"
cask "visual-studio-code"

# --- fonts (powerline glyphs for the agnoster zsh theme) ---
cask "font-meslo-lg-nerd-font"

# --- AI tools ---
brew "herdr"
brew "crit"
